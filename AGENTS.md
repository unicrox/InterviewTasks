# Easy to read and maintain:

- Keep one-off logic in the main code flow instead of extracting a standalone function only to label its purpose. If the goal is to explain the idea behind that section, add a concise comment(with prefix `-- `) above the code instead.


